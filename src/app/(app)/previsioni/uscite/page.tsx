import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function PrevisioniUscitePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Previsioni Uscite</CardTitle>
        <CardDescription>
          Questa pagina mostrerà le previsioni sulle uscite future.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Contenuto in costruzione...</p>
      </CardContent>
    </Card>
  );
}
