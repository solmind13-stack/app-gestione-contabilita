import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function PrevisioniEntratePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Previsioni Entrate</CardTitle>
        <CardDescription>
          Questa pagina mostrerà le previsioni sulle entrate future.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Contenuto in costruzione...</p>
      </CardContent>
    </Card>
  );
}
