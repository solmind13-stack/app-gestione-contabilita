import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function MovimentiPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Movimenti</CardTitle>
        <CardDescription>
          Questa pagina mostrerà l'elenco di tutti i movimenti finanziari.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Contenuto in costruzione...</p>
      </CardContent>
    </Card>
  );
}
