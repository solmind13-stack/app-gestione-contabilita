import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function ScadenzePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Scadenze</CardTitle>
        <CardDescription>
          Questa pagina mostrerà le scadenze imminenti e passate.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Contenuto in costruzione...</p>
      </CardContent>
    </Card>
  );
}
