import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function AssistenteAiPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Assistente AI</CardTitle>
        <CardDescription>
          Questa pagina conterrà l'interfaccia di chat con l'assistente AI.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Contenuto in costruzione...</p>
      </CardContent>
    </Card>
  );
}
